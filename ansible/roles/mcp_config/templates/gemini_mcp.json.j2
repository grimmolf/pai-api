{
  "mcpServers": {
    "{{ mcp_server_name }}": {
      "command": "{{ deploy_dir }}/run_mcp.fish",
      "args": [],
      "timeout": {{ mcp_timeout }},
      "description": "{{ mcp_server_description }}"
    }
  }
}
